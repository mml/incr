MACH = arm32le

C = $(CC) $(CPPFLAGS) -march=armv8-a+simd -mno-sched-prolog -DARM32LE $(CFLAGS)

%.s: %.c
	$(C) -O3 --omit-frame-pointer -S -o $@ $<

%.o: %.s
	$(AS) -o $@ $<

%.dump: %.o
	$(OBJDUMP) -D $< > $@

%: %.o
	$(CC) -static -o $@ $<

include Mf-base
